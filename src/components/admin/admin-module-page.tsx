import { Search, SlidersHorizontal } from "lucide-react";
import { adminModules, type AdminModuleKey } from "@/lib/admin/registry";
export function AdminModulePage({ module }: { module: AdminModuleKey }) { const item = adminModules[module]; return <main className="mx-auto max-w-[1500px] px-4 py-8 sm:px-8"><p className="text-sm font-semibold text-violet-600">Platform Admin</p><h1 className="mt-2 text-3xl font-semibold">{item.title}</h1><p className="mt-2 max-w-3xl text-muted-foreground">{item.description}</p><div className="mt-7 flex flex-col gap-3 sm:flex-row"><label className="flex h-11 flex-1 items-center gap-3 rounded-xl border bg-card px-4"><Search className="size-4 text-muted-foreground" /><input className="min-w-0 flex-1 bg-transparent text-sm outline-none" placeholder={`Pretraži: ${item.title.toLowerCase()}…`} /></label><button className="inline-flex h-11 items-center justify-center gap-2 rounded-xl border bg-card px-4 text-sm font-medium"><SlidersHorizontal className="size-4" />Filteri</button></div><section className="mt-5 rounded-3xl border bg-card"><div className="border-b px-5 py-4 text-sm font-semibold">Rezultati · server-side paginacija 25 po stranici</div><div className="p-12 text-center"><p className="font-medium">{item.empty}</p><p className="mt-2 text-sm text-muted-foreground">Produkcijski podaci pojavljuju se tek nakon migracije i provjere admin dozvole.</p></div></section></main>; }


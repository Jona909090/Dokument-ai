"use client";import type{DocumentType}from"@/lib/document-types";import{templateCss}from"@/lib/templates/engine";import{useTemplateEngine}from"./template-provider";export function TemplateSurface({type,children,className=""}:{type:DocumentType;children:React.ReactNode;className?:string}){const{resolve}=useTemplateEngine(),template=resolve(type);return <div data-template-id={template.id} className={`template-surface ${className}`} style={templateCss(template)}>{children}</div>}

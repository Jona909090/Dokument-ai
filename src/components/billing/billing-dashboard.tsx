import Link from "next/link";
import { AlertTriangle, CheckCircle2, CreditCard, FileText, ReceiptText } from "lucide-react";
import { PortalButton } from "./billing-actions";
import { PlanBadge, UsageMeter } from "./feature-gates";
import { SubscriptionActions } from "./subscription-actions";
import { getPlan } from "@/lib/billing/plans";
import type { PlanId } from "@/lib/billing/types";
export function BillingDashboard({ planId = "free", provider = "mock", status = "free" }: { planId?: PlanId; provider?: "stripe" | "mock"; status?: string }) { const plan = getPlan(planId); const demo = provider === "mock"; return <main className="mx-auto max-w-6xl px-4 py-8 sm:px-8"><div className="flex flex-wrap items-center gap-3"><p className="text-sm font-semibold text-primary">Naplata i pretplata</p>{demo && <span className="rounded-full bg-amber-500/10 px-3 py-1 text-xs font-semibold text-amber-700 dark:text-amber-300">Demo naplata · nema stvarnih uplata</span>}</div><div className="mt-2 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-semibold">Vaš {plan.name} paket</h1><p className="mt-2 text-muted-foreground">Status: {status}. Prava se potvrđuju iz baze i Stripe webhook projekcije.</p></div><PlanBadge planId={planId} /></div>{status === "past_due" && <div className="mt-6 flex gap-3 rounded-2xl border border-red-200 bg-red-50 p-4 text-red-800"><AlertTriangle className="size-5" /><div><b>Plaćanje nije uspjelo</b><p className="text-sm">Ažurirajte način plaćanja. Podaci se ne brišu tijekom grace perioda.</p></div></div>}<section className="mt-7 grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><article className="rounded-3xl border bg-card p-6"><div className="flex items-center gap-3"><CreditCard className="size-5 text-primary" /><h2 className="text-lg font-semibold">Pretplata</h2></div><dl className="mt-5 grid gap-4 text-sm sm:grid-cols-2"><div><dt className="text-muted-foreground">Paket</dt><dd className="font-semibold">{plan.name}</dd></div><div><dt className="text-muted-foreground">Obnova</dt><dd className="font-semibold">Nije potvrđena u demo načinu</dd></div><div><dt className="text-muted-foreground">Način plaćanja</dt><dd className="font-semibold">Nema spremljenih kartičnih podataka</dd></div><div><dt className="text-muted-foreground">Porez</dt><dd className="font-semibold">Stripe Tax nije konfiguriran</dd></div></dl><div className="mt-6 flex flex-wrap gap-3"><PortalButton /><Link href="/pricing" className="inline-flex h-10 items-center rounded-md bg-primary px-4 text-sm font-medium text-primary-foreground">Promijeni paket</Link></div><div className="mt-6 border-t pt-6"><SubscriptionActions /></div></article><article className="rounded-3xl border bg-card p-6"><h2 className="flex items-center gap-2 font-semibold"><CheckCircle2 className="size-5 text-emerald-500" />Potrošnja ovog mjeseca</h2><div className="mt-5 space-y-5"><UsageMeter label="Dokumenti" current={0} limit={plan.limits.documents} /><UsageMeter label="AI akcije" current={0} limit={plan.limits.aiActions} /><UsageMeter label="PDF izvozi" current={0} limit={plan.limits.pdfExports} /><UsageMeter label="DOCX izvozi" current={0} limit={plan.limits.docxExports} /></div></article></section><section className="mt-6 rounded-3xl border bg-card p-6"><h2 className="flex items-center gap-2 text-lg font-semibold"><ReceiptText className="size-5 text-primary" />Računi</h2><div className="mt-5 rounded-2xl border border-dashed p-10 text-center"><FileText className="mx-auto size-7 text-muted-foreground" /><p className="mt-3 font-medium">Još nema računa</p><p className="mt-1 text-sm text-muted-foreground">Stripe računi pojavit će se nakon sigurnog webhook sinkroniziranja.</p></div></section></main>; }


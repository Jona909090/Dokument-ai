import type { LucideIcon } from "lucide-react";
import type { RankedMetric, TrendPoint } from "@/lib/analytics/types";

export function MetricCard({ label, value, note, Icon }: { label: string; value: string | number; note: string; Icon: LucideIcon }) { return <article className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-start justify-between"><p className="text-sm text-muted-foreground">{label}</p><span className="rounded-xl bg-primary/10 p-2 text-primary"><Icon className="size-4" /></span></div><p className="mt-3 text-3xl font-semibold tracking-tight">{value}</p><p className="mt-1 text-xs text-muted-foreground">{note}</p></article>; }

export function RankedList({ title, rows, suffix = "" }: { title: string; rows: RankedMetric[]; suffix?: string }) { const max = Math.max(...rows.map((row) => Math.abs(row.value)), 1); return <section className="rounded-2xl border bg-card p-5"><h2 className="font-semibold">{title}</h2><div className="mt-5 space-y-4">{rows.length ? rows.map((row,index) => <div key={row.key}><div className="mb-1.5 flex items-center gap-3 text-sm"><span className="w-5 text-xs font-semibold text-muted-foreground">{index + 1}</span><span className="min-w-0 flex-1 truncate">{row.label}</span><span className="font-semibold">{row.value}{suffix}</span></div><div className="ml-8 h-1.5 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-gradient-to-r from-blue-600 to-indigo-500" style={{ width: `${Math.max(4, Math.abs(row.value) / max * 100)}%` }} /></div>{row.secondary && <p className="ml-8 mt-1 text-[11px] text-muted-foreground">{row.secondary}</p>}</div>) : <p className="py-8 text-center text-sm text-muted-foreground">Nema podataka za odabrane filtere.</p>}</div></section>; }

export function TrendChart({ title, data }: { title: string; data: TrendPoint[] }) { const max = Math.max(...data.map((point) => point.value), 1); return <section className="rounded-2xl border bg-card p-5"><div className="flex items-center justify-between"><h2 className="font-semibold">{title}</h2><span className="text-xs text-muted-foreground">Broj događaja</span></div><div className="mt-6 flex h-48 items-end gap-1.5" role="img" aria-label={`${title}, stupčasti prikaz`}>{data.map((point) => <div key={point.label} className="group relative flex min-w-0 flex-1 flex-col items-center justify-end"><span className="pointer-events-none absolute -top-7 hidden rounded bg-slate-950 px-2 py-1 text-[10px] text-white group-hover:block">{point.label}: {point.value}</span><div className="w-full rounded-t bg-gradient-to-t from-blue-600 to-indigo-400 transition hover:from-blue-500" style={{ height: `${Math.max(4, point.value / max * 100)}%` }} /><span className="mt-2 hidden max-w-full truncate text-[9px] text-muted-foreground 2xl:block">{point.label.slice(-5)}</span></div>)}</div></section>; }

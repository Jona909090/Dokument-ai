import{z}from"zod";export const uuid=z.string().uuid();export const profileInput=z.object({first_name:z.string().trim().max(100),last_name:z.string().trim().max(100),display_name:z.string().trim().max(150),phone:z.string().trim().max(40),country:z.string().trim().max(80),city:z.string().trim().max(120),language:z.enum(["hr","en"]),timezone:z.string().trim().max(80),preferred_currency:z.string().trim().length(3),date_format:z.string().trim().max(40),number_format:z.string().trim().max(40)});export const organizationInput=z.object({name:z.string().trim().min(2).max(160),slug:z.string().trim().regex(/^[a-z0-9-]+$/).max(100),country:z.string().trim().max(80),default_language:z.enum(["hr","en"]),default_currency:z.string().length(3)});export const cloudDocumentInput=z.object({id:uuid.optional(),organization_id:uuid,company_id:uuid.nullable(),contact_id:uuid.nullable(),project_id:uuid.nullable(),source_document_id:uuid.nullable(),document_type:z.string().min(1).max(80),document_category:z.string().min(1).max(80),document_number:z.string().max(100),title:z.string().trim().min(1).max(200),status:z.string().max(40),language:z.enum(["hr","en"]),currency:z.string().length(3),template_id:uuid.nullable(),visibility_profile_id:uuid.nullable(),form_data:z.record(z.string(),z.unknown()),rendered_content:z.record(z.string(),z.unknown()),subtotal_minor:z.number().int().safe(),tax_minor:z.number().int().safe(),total_minor:z.number().int().safe(),version:z.number().int().positive()});export const attachmentInput=z.object({organizationId:uuid,documentId:uuid,fileName:z.string().min(1).max(255),mimeType:z.enum(["image/png","image/jpeg","image/webp","image/svg+xml","application/pdf","application/vnd.openxmlformats-officedocument.wordprocessingml.document"]),size:z.number().int().positive().max(52_428_800)});

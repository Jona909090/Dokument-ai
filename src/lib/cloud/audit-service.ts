import type{SupabaseClient}from"@supabase/supabase-js";const allowedKeys=new Set(["status","role","format","document_type","template_id","reason_code"]);export class AuditService{constructor(private client:SupabaseClient){}async record(input:{organizationId:string;actorUserId:string;action:string;entityType:string;entityId?:string;metadata?:Record<string,string|number|boolean|null>}){const safeMetadata=Object.fromEntries(Object.entries(input.metadata??{}).filter(([key])=>allowedKeys.has(key)));const{error}=await this.client.from("audit_logs").insert({organization_id:input.organizationId,actor_user_id:input.actorUserId,action:input.action.slice(0,100),entity_type:input.entityType.slice(0,80),entity_id:input.entityId,safe_metadata:safeMetadata});if(error)throw error}}

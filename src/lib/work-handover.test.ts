import{describe,expect,it}from"vitest";import{calculateHandoverSummary,createHandoverRecord,createWorkHandover}from"@/lib/work-handover";
describe("work handover",()=>{it("uses current year and safe empty state",()=>{const d=createWorkHandover();expect(d.number).toContain(String(new Date().getFullYear()));expect(calculateHandoverSummary(d).openDefects).toBe(0)});it("supports more than 20 participants and 50 defects",()=>{const d=createWorkHandover();d.sections.participants=Array.from({length:21},()=>createHandoverRecord("participants"));d.sections.defects=Array.from({length:51},()=>createHandoverRecord("defects"));expect(calculateHandoverSummary(d)).toMatchObject({participants:21,openDefects:51})});it("tracks accepted works and closed defects",()=>{const d=createWorkHandover(),w=createHandoverRecord("works"),defect=createHandoverRecord("defects");w.fields["status pregleda"]="prihvaćeno";defect.fields.status="zatvoreno";d.sections.works=[w];d.sections.defects=[defect];expect(calculateHandoverSummary(d)).toMatchObject({works:1,accepted:1,closedDefects:1,openDefects:0})});it("preserves Croatian Unicode",()=>{const d=createWorkHandover();d.subject="Čišćenje, završni radovi i ključ";expect(d.subject).toContain("ključ")})});
